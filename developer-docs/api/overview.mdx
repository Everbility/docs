---
title: "API overview"
description: "Reference overview for the Everbility Public API."
---

## Base URL

```text
https://api.everbility.com/v1/public
```

## Authentication

All endpoints use bearer authentication with an Everbility API key:

```http
Authorization: Bearer <api_key>
```

Ask an organisation admin to enable API keys for your organisation. They can email [support@everbility.com](mailto:support@everbility.com) to enable API keys and/or create a test account.

## Response patterns

- Synchronous endpoints return JSON.
- PDF and large-audio uploads return a `job_id` and complete asynchronously.
- Report generation returns a streamed sequence of JSON lines.

## Machine-readable schema

You can also fetch the raw OpenAPI schema from:

```text
https://api.everbility.com/v1/public/openapi.json
```

Use the navigation on this tab to explore each endpoint in detail.
